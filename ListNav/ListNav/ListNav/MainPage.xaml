<?xml version="1.0" encoding="utf-8" ?>
<ContentPage Title="" xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:ListNav"
             x:Class="ListNav.MainPage">

    <ContentPage.Content>
        <StackLayout>
    <ListView x:Name="listView" ItemsSource="{Binding ListItems}">
        <ListView.ItemTemplate>
            <DataTemplate>
                <ImageCell x:Name="imgcel" ImageSource="{Binding Source}" Text="{Binding Title}" Detail="{Binding Description}" />
            </DataTemplate>
        </ListView.ItemTemplate>
    </ListView>
        </StackLayout>
    </ContentPage.Content>
    

</ContentPage>